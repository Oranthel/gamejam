# 《忆术》项目状态交接文档

> 用途：上下文若被清空，把本文件喂给 AI 即可恢复全部项目状态。
> 更新时间：Game Jam 冲刺期（最后一次同步）。

## 1. 项目基本信息

- 游戏：《忆术》——美术高考集训 + 精神「侵蚀」可视化的文字剧情游戏（galgame 式）
- 引擎：**Godot 4.7**（GDScript），GL Compatibility 渲染，1920x1080
- 项目路径：`E:\AI-Design-OS\Deepseek harness\gamejam`
- 主场景：`menu/MainMenu.tscn`（开始/继续/剧情树/退出）
- 中文字体：已内嵌 Noto Sans SC（`fonts/NotoSansSC.ttf` + `fonts/theme.tres`，`project.godot` 挂了 `gui/theme/custom`）

## 2. 数值系统（已锁定）

| 值 | 规则 | 说明 |
|---|---|---|
| 侵蚀值 | 初始 10，**满 70 = 坏结局**（黄粱一梦） | ==压力值；压力选项加、休息选项可负 |
| 心情值 | 初始 0，范围 -10~10，**每天结束归零** | 结算时 `侵蚀 -= 心情` |
| 能力值 | 初始 5，范围 0~30，**≥22 = 金榜题名** | 下限 0，负值选项只减到 0 |

- 坏结局阈值：`GameState.gd` 的 `EROSION_MAX = 70`
- 上岸阈值：`GameState.gd` 的 `ABILITY_UP_THRESHOLD = 22`（能力上限 `ABILITY_MAX = 30`）
- 心情归零：`GameState.settle_mood()` 里 `mood = 0`

## 3. 剧情数据

- `剧情JSON_主线版.json`：**8 天（Day1~7 + 结局），141 事件**
- 天数链：70→60→50→40→30→20→10 天 → 离开画室 → 结局判定
- 结局：金榜题名（能力≥22）/ 芸芸众生（能力<22）/ 黄粱一梦（侵蚀=70）
- 选项数值已从 `选项数值表.xlsx` 同步（含负值，52 个选项）
- 新事件类型：`choice_goto`（按之前选项跳转，`StoryManager.gd`）

## 4. UI 系统（5 档侵蚀）

- `systems/ErosionTheme.gd`：档位 = max(侵蚀/20, 天数档)；调色板 + 图片 fallback
- 档位：0 正常 / 1 褪色 / 2 黑白 / 3 蓝晒 / 4 残稿
- 图片 fallback：`art/ui/dlg_X.png`、`bar_X.png`（九宫格 24px，没图退回颜色）
- 破碎线条：`systems/ErosionSketchLines.gd`（`_draw()` 动态画线，参数：`cycle_seconds`/`phase_offset`/`rng_seed`/`light_lines`/`shade_override`/`width_scale`）

## 5. 已完成功能

- ✅ 主菜单（封面 + 4 按钮，每个按钮有破碎线条框，节奏/样式各自错开）
- ✅ 3 存档位（存档/读档/**删除** 三种模式，档位显示"距离联考X天·第N天"）
- ✅ 历史回看（顶部「回看」按钮，滚动面板显示全部对话）
- ✅ 剧情树文字版（`menu/StoryTree.gd` 自动从 JSON 生成，未解锁选项显示？？？，右上「重置解锁」）
- ✅ 结局面板（黑幕 + 结局名 + 「回到一切的开始」按钮，`act2/Act2.gd`）
- ✅ 侵蚀 UI 变色 + 数值飘字 + 开场的 EraseEffect 导出修复
- ✅ 开场动画（intro，scene1-10.jpg）→ Act1（捡画包）→ Act2（对话系统）
- ✅ 关键节点背景切换（17 处 + 3 结局 CG，机制见「场景对应」专题）
- ✅ 成绩公布中文化（`Act2._on_result` 显示「分数/排名：第X名」）
- ✅ 3 张结局 CG 接入（金榜题名/芸芸众生/黄粱一梦，`Act2.gd` bg_map 的 BG_END_UP/SELF/BAD）
- ✅ 背景音乐接入（无水印版，`systems/AudioManager.gd` autoload：开头/日常/金榜题名/芸芸众生+黄粱一梦；换曲交叉过渡：新曲淡入 3s、旧曲淡出 5s）
- ✅ 音效接入（Kenney UI Audio：click 点击 + rollover 悬停；主菜单/选项按钮有悬停声，主菜单/选项/对话推进/游戏内菜单/回看有点击声；悬停声已调低 20%，在 `AudioManager.sfx_hover_volume_db`）
- ✅ 顶部「菜单/回看」按钮跟随 5 档侵蚀主题（`StatusBar._apply_button_theme`），宽度加宽 10%（`_widen_top_buttons`）
- ✅ 删除开场「请绘制→画作完成」段落（无绘画玩法，Day1 开场改为直接推门进画室，`SCENE_D01_START` 只剩 EV_D01_002 过渡）
- ✅ 转场/公布文字加淡灰径向渐变底板（`Act2._setup_transition_backdrop`，中心不透明→边缘透明，避免白字与背景融合）

## 6. 待办 / 已知问题

- ✅ **关键节点背景切换已做**（17 处，见下方「场景对应」专题）：机制已实现，重点节点（宿舍↔画室、吃饭↔画室、周考/晚课回画室、楼梯抽烟、水房）已切；走廊已补图，水房/超市用画室图替代。
- ⬜ **NPC 名字配色**：李安（CHAR_FRIEND）浅蓝、小草（CHAR_A）浅黄；赵北（CHAR_B）在 JSON 里 0 句台词，不用配。改 `DialogueBox._show_current` 的 `_base_name_color`（os 已用 `.darkened(0.35)`）。用户说「朋友用得多就先不搞」，暂缓。
- ✅ **音乐/音效已接入**：BGM（无水印版，开头/日常/金榜题名/芸芸众生+黄粱一梦，交叉过渡 3s 淡入/5s 淡出）+ 点击/悬停音效，见 `systems/AudioManager.gd`。
- ⬜ 美术素材缺：结局通用图（BG_ENDING）仍缺（但 3 个结局各自有 CG，进入结局即切 CG，不露待定图）
- ✅ 3 张结局 CG 全部接入（金榜题名→BG_END_UP、芸芸众生→BG_END_SELF、黄粱一梦→BG_END_BAD）
- ⚠️ 需在编辑器 F5 验证最新改动（本机 headless 校验会因编辑器占用而卡住）

### 场景对应（bg 切换）实现方案（已实现关键节点）

- **机制**：`StoryManager.gd` 的 `_process_event` 里，事件若带 `background` 字段，先 `scene_change_requested.emit([bg_id])` 再展示；`Act2.gd` 已有 `_on_scene_change` 接信号换背景。另支持 `"bg"` 事件类型（等价，读到即切背景并 advance）。
- **用法**：在 `剧情JSON_主线版.json` 的事件对象里加一行 `"background": "BG_XXX"`（BG id 见 `Act2.gd` 的 `bg_map`），不用改 next 指针。
- **已切的 17 处关键节点**（全部用有图的背景；走廊已补图，水房/超市映射到画室图）：
  - Day1 `EV_D01_007`→BG_DINING_TABLE（午饭）、`EV_D01_DORM`→BG_DORM_MAP（回寝）
  - Day2 `EV_D02_NOON_STUDIO`→BG_STUDIO
  - Day3 `EV_D03_002`→BG_WATER_ROOM（水房抽烟，实为画室图）、`EV_D03_STUDIO`→BG_STUDIO、`EV_D03_WEEKLY_EXAM`/`EV_D03_LATE_CLASS`→BG_EASEL_VIEW、`EV_D03_DINNER`→BG_DORM_MAP
  - Day4 `EV_D04_003`→BG_EASEL_VIEW（宿舍→画室）、`EV_D04_STAIRS`→BG_STUDIO_CORRIDOR（楼梯抽烟，走廊）
  - Day5 `EV_D05_CLASS`→BG_STUDIO、`EV_D05_WASH`→BG_WATER_ROOM（恶心，实为画室图）
  - Day6 `EV_D06_003`→BG_EASEL_VIEW、`EV_D06_REVIEW`→BG_STUDIO（吃饭→画室评画，20天那个 bug）、`EV_D06_DORM`→BG_DORM_MAP
  - Day7 `EV_D07_003`→BG_STUDIO
  - 结局 `EV_END_UP`→BG_END_UP（金榜题名 CG）、`EV_END_SELF`→BG_END_SELF（芸芸众生 CG）、`EV_END_BAD`→BG_END_BAD（黄粱一梦 CG）
- **妈妈电话场景**：Day3 `SCENE_D03_PHONE_CALL` 用 `BG_STUDIO_CORRIDOR`（走廊已补图 `art/场景/BG_STUDIO_CORRIDOR.png`）。
- **成绩公布中文化**：`Act2._on_result` 改为 `分数：X` / `排名：第 X 名`（原来是 `score: X` / `rank: X`）。
- 台词分布（从 JSON 统计）：主角 77 / 主角(OS) 131 / 朋友 53 / 小草 14 / 赵北 0 / 老师 37 / 妈妈 12 / 旁白 15。

## 7. 技术坑（务必记住）

1. **警告视为错误**：禁止 `var x := max(...)` 这类 Variant 推断（`max/min` 返回 Variant），必须显式类型 `var s: float = max(...)`
2. **headless 会卡**：用户 Godot 编辑器开着时，命令行 Godot（--check-only/--editor）拿不到文件锁会挂起——此时靠代码审查 + 用户 F5 验证
3. **GLM 识图限流**：`mcp__glm-vision` 经常 429，读图不可靠，尽量用文字描述
4. 素材自动识别扩展名（png/jpg/jpeg/webp），同名覆盖即可热替换

## 8. 素材清单（详见 `gamejam/素材命名清单.md`、`缺少图片表.md`）

- 场景图：走廊已补（`art/场景/BG_STUDIO_CORRIDOR.png`）；水房/超市用画室图替代（`Act2.gd` bg_map 已映射到 BG_STUDIO）；3 张结局 CG 已补（`art/场景/BG_END_UP/SELF/BAD.png`）；结局通用图 BG_ENDING 仍缺
- 主角立绘 ✅、封面 ✅（`art/封面.jpg`，注意是 jpg！）
- 结局 CG ×3 ✅、道具图可选
- 音频（`audio/`，均无水印版）：`bgm_opening.mp3`（开头）、`bgm_daily.mp3`（日常）、`bgm_end_up.mp3`（金榜题名）、`bgm_end_down.mp3`（芸芸众生+黄粱一梦）、`sfx_click.ogg`（点击）、`sfx_hover.ogg`（悬停）；统一由 `systems/AudioManager.gd`（autoload）播放：`AudioManager.play_bgm(...)` / `play_sfx(...)`，淡入 3s / 淡出 5s 可在 `fade_in_duration`/`fade_out_duration` 调整。Kenney 音效包原文件在 `参考图/场景图/画桌/kenney_ui-audio/Audio/`（含 click1-5、rollover1-6 悬停、switch1-38 切换）。
