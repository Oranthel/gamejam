# 《忆术》数值与结局 · 定稿规范（v1）

> 本文件是给全组（策划/程序/美术）对齐的唯一事实来源。改数值请改这里 + 对应代码常量，两边同步。

## 1. 三个隐藏值（已锁定）

| 值 | 含义 | 初始 | 范围 | 作用 |
|---|---|---|---|---|
| **侵蚀值 erosion** | == 压力值。压力类选项直接增加侵蚀 | 10 | 0 ~ 100 | 满 100 立即触发**坏结局**；也驱动 UI 侵蚀阶段 |
| **心情值 mood** | 情绪状态 | 0 | -10 ~ 10 | 每天/阶段结束结算：正→减侵蚀，负→增侵蚀 |
| **能力值 ability** | 专业成绩 | 5 | 0 ~ 20 | 结局判定「上岸」用 |

> 旧 JSON 里 `effects` 的 `"stress"` 字段已废弃，统一改用 `"erosion"`。代码里 `GameState.apply_effects()` 仍兼容 `"stress"`（自动当成 erosion），所以旧文件不会崩。

## 2. 心情结算机制（本轮实现的是这条）

每天结束（`type: "sleep"` 事件）自动执行：

```
侵蚀值 = clamp( 侵蚀值 - 心情值, 0, 100 )
```

- 心情为正 → 侵蚀回落（越放松越能挺住）
- 心情为负 → 侵蚀上升（越丧越被吞噬）

触发点 = 一天的结尾 `sleep` 事件（代码里 `GameState.settle_mood()` 已挂在 `sleep` 处理器里）。

## 3. 心情值「锁选项」机制（预留，本轮不做）

想法：心情过低时某些高投入选项置灰/被替换成「我不想去了……」。

- 已在规范预留，JSON 未来可在选项上写 `"min_mood": -5` 表示「心情低于 -5 时该选项不可选」。
- 本轮**不实现**，因为要写替补文案 + 改选项 UI，5 天性价比低。等 Day2-5 补齐后再加。

## 4. 结局（三个）

| 结局 | 触发 | 含义 |
|---|---|---|
| 坏结局「蚀穿」 | 侵蚀值 = 100（实时，随时触发）| 被压力彻底蚀穿 |
| 好结局「上岸」 | 结尾结算时 能力值 ≥ 12 | 考上理想大学，但创作初心被磨平 |
| 好结局「初心」 | 结尾结算时 能力值 < 12 | 没考到最好，但重新找回画画的理由 |

- 坏结局：`GameState.erosion_maxed` 信号 → `StoryManager` 直接跳 `EV_END_BAD`（无视当前位置）。
- 两个好结局：最后一天结束走到 `type: "ending_check"` 事件，按能力值二分。

## 5. 内心独白（os）与旁白的 JSON 写法

- 内心独白：`"speaker": "CHAR_MAIN_OS"`（任何 `*_OS` 后缀都行）→ UI 显示名字「内心」+ 灰蓝色斜体感文字。
- 旁白：`"speaker": "NARRATOR"` → UI 显示「旁白」。
- 不要在 text 里手写 `os:` 前缀（旧 JSON 的问题，已修）。

## 6. 主线 JSON 格式（样本）

- 只用三种事件跑主线：`dialogue`（对话）/ `option`（选项）/ `transition`（转场）。
- 另有：`sleep`（一天结束，含心情结算）、`ending_check`（结局分岔）、`ending`（结局演出）。
- 旧 JSON 里的小游戏/探索/交互/吃饭/拿道具等事件类型，程序已「自动跳过」，新 JSON 直接不写它们。

## 7. 当前进度 / 待办

- ✅ 样本 JSON `剧情JSON_主线版.json`：**Day1 完整 + 三个结局**（可运行，供团队对齐格式与机制）。
- ⬜ Day2 / Day3：从旧 JSON 迁移文本 + 对齐最新《剧情.doc》。
- ⬜ Day4（40天）/ Day5（30天）：从《剧情.doc》补。
- ⬜ 结局文案为草案，编剧可润色。

## 8. 调参备忘（写代码的人看）

- 坏结局阈值 = `GameState.EROSION_MAX`（当前 100）。**注意：目前选项压力只有 +1/+2，5 天很难自然冲到 100**，建议联调时要么把阈值降到 60~70，要么加大压力选项数值/心情结算幅度。这是已知待调项。
- 上岸阈值 = `GameState.ABILITY_UP_THRESHOLD`（当前 12）。
